<?xml version="1.0" encoding="UTF-8"?>
<configuration packagingData="true">
    <!-- .............................. Developer and Testing Logger Configuration ................................. -->
    <springProfile name="dev">
        <!-- property for another OS -->
        <property name="DEV_HOME" value="c:/logs"/>
        <!--- Console logger viewer -->
        <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <layout class="ch.qos.logback.classic.PatternLayout">
                <Pattern>
                    %d{yyyy-MM-dd HH:mm:ss} [%thread] %highlight(%-5level) %cyan(%logger{36}.%M %L) - %msg %n
                </Pattern>
            </layout>
        </appender>
        <!-- File logger viewer -->
        <appender name="FILE-AUDIT" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${DEV_HOME}/debug.log</file>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <Pattern>
                    %d{yyyy-MM-dd HH:mm:ss} [%thread] %highlight(%-5level) %cyan(%logger{36}.%M %L) - %msg %n
                </Pattern>
            </encoder>
            <!-- rollover daily -->
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${DEV_HOME}/archived/debug.%d{yyyy-MM-dd}.%i.log
                </fileNamePattern>
                <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                    <maxFileSize>10MB</maxFileSize>
                </timeBasedFileNamingAndTriggeringPolicy>
            </rollingPolicy>
        </appender>
        <!-- File logger viewer ERROR -->
        <appender name="FILE-ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${DEV_HOME}/error.log</file>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <Pattern>
                    %d{yyyy-MM-dd HH:mm:ss} [%thread] %highlight(%-5level) %cyan(%logger{36}.%M %L) - %msg %n
                </Pattern>
            </encoder>
            <!-- rollover daily -->
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${DEV_HOME}/archived/error.%d{yyyy-MM-dd}.%i.log
                </fileNamePattern>
                <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                    <maxFileSize>10MB</maxFileSize>
                </timeBasedFileNamingAndTriggeringPolicy>
            </rollingPolicy>

        </appender>
        <!-- Configuration logger for spring mongo redis add and etc  -->
        <logger name="org.mongodb.driver.cluster" level="OFF"/>
        <!-- root level logger -->
        <root level="debug">
            <appender-ref ref="FILE-ERROR"/>
            <appender-ref ref="STDOUT"/>
        </root>

    </springProfile>

    <!-- .............................. Production Logger Configuration ................................................ -->

    <springProfile name="pre-prod">

        <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <layout class="ch.qos.logback.classic.PatternLayout">
                <Pattern>
                    %d{yyyy-MM-dd HH:mm:ss} [%thread] %highlight(%-5level) %cyan(%logger{36}.%M %L) - %msg %n
                </Pattern>
            </layout>
        </appender>

        <!-- Configuration logger for spring mongo redis add and etc  -->
        <logger name="ua.com.gup" level="debug" additivity="false">
            <appender-ref ref="STDOUT"/>
        </logger>

        <logger name="org.mongodb.driver.cluster" level="OFF"/>

        <logger name="org.springframework" level="debug" additivity="false">
            <appender-ref ref="STDOUT"/>
        </logger>

        <root level="info">
            <appender-ref ref="STDOUT"/>
        </root>
    </springProfile>

</configuration>